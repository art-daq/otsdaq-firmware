component flip_flop
	port (
		clock: in STD_LOGIC;
		d: in STD_LOGIC;
		reset_n: in STD_LOGIC;
		q: out STD_LOGIC);
end component;


instance_name : flip_flop
( clock => ,
  d => ,
  q => ,
  reset_n => );
